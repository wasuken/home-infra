# 単一ルータ構成用keepalived設定
vrrp_instance VI_1 {
    state MASTER
    interface {{ internal_interface }}
    virtual_router_id {{ virtual_router_id }}
    priority 100
    advert_int 1
    virtual_ipaddress {
        {{ virtual_ip }}/24
    }
    # 単一ルータ時は常にVIPを保持
    nopreempt
}
